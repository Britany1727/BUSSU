name: bussu
description: "Sistema de gestión y monitoreo de transporte público Bussu"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  flutter_riverpod: ^2.6.1
  go_router: ^15.1.3
  get_it: ^8.0.3

  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0

  supabase_flutter: ^2.8.4

  mqtt_client: ^10.6.0

  google_maps_flutter: ^2.11.0

  connectivity_plus: ^6.1.4
  device_info_plus: ^11.4.1

  dartz: ^0.10.1

  web_socket_channel: ^3.0.2
  intl: ^0.20.2
  crypto: ^3.0.6
  flutter_secure_storage: ^9.2.4
  flutter_map: ^8.3.1
  latlong2: ^0.10.1
  http: ^1.6.0
  path_provider: ^2.1.6
  geolocator: ^14.0.2
  permission_handler: ^11.4.0
  image_picker: ^1.1.2
  flutter_local_notifications: ^18.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  freezed: ^3.0.6
  json_serializable: ^6.9.5
  build_runner: ^2.4.15

  flutter_lints: ^6.0.0

  mocktail: ^1.0.4

  flutter_launcher_icons: ^0.14.3

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/icon.jpeg"

flutter:
  uses-material-design: true
